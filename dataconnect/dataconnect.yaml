specVersion: "v1beta"
serviceId: "front"
location: "us-central1"
schema:
  source: "./schema"
  datasource:
    postgresql:
      database: "fdcdb"
      cloudSql:
        instanceId: "front-fdc"
      # schemaValidation: "COMPATIBLE"
connectorDirs: ["./connector"]
